import { Pool } from 'pg';

const pool = new Pool({
    host: 'localhost', // 'localhost' is the default;
    port: 5432, // 5432 is the default;
    database: 'database_name',
    user: 'user_name',
    password: '******',
});

export default pool;
